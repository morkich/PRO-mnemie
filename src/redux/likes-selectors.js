export const getLikesData = (state) => {
  return state.likes.likesData;
}

export const getLikesCoint = (state) => {
  return state.likes.likesCoint;
}

export const getOnLike = (state) => {
  return state.likes.onLike;
}

export const getOnDislike = (state) => {
  return state.likes.onDislike;
}

export const getLikeLoading = (state) => {
  return state.likes.likeLoading;
}