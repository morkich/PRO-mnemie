export const getProfileDataItemsState = (state) => {
  return state.profileData.profileDataItems;
}

export const getProfileDataItemsCount = (state) => {
  return state.profileData.profileDataItemsCount;
}

export const getProfileDataExpertName = (state) => {
  return state.profileData.profileDataExpertName;
}

export const getProfileDataLoadingState = (state) => {
  return state.profileData.profileDataLoading;
}
