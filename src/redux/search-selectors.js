export const getSearchQueryState = (state) => {
  return state.search.searchQuery;
}

export const getSearchResultState = (state) => {
  return state.search.searchResult;
}

export const getSearchLoadingState = (state) => {
  return state.search.sarchLoading;
}
