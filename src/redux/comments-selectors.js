export const getCommentsState = (state) => {
  return state.comments.comments;
}

export const getCommentUserIdState = (state) => {
  return state.comments.userId;
}

export const getCommentLoadingState = (state) => {
  return state.comments.loading;
}

export const getCommentUserAvatarState = (state) => {
  return state.comments.userAvatar;
}

export const getCommentUserNameState = (state) => {
  return state.comments.userName;
}

export const getCommentPostIdState = (state) => {
  return state.comments.postId;
}