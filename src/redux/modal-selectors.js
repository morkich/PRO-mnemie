export const getModalTypeState = (state) => {
  return state.modal.typeModal;
}

export const getModalVisionState = (state) => {
  return state.modal.vision;
}

export const getModalLoadingState = (state) => {
  return state.modal.loadingModal;
}