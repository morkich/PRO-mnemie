export const getfiltersItems = (state) => {
  return state.filters.filterItems;
}

export const getfilterTabQuery = (state) => {
  return state.filters.filterTabQuery;
}

export const getFilterLoading = (state) => {
  return state.filters.filterLoading;
}